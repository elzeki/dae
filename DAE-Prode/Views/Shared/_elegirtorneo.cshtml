@model DAE_Prode.Models.torneomodels
@using (Html.BeginForm())
{
<fieldset>
            <legend>Seleccionar Torneo</legend>

            @{ var aux1 = (IEnumerable<DAE_Prode.Models.torneomodels>)ViewData["listatorneos"]; }

             <div class="editor-label">
                @Html.LabelFor(m => m.nombre)
            </div>
            @Html.DropDownListFor(
                        m => m.id,
                        new SelectList(aux1, "id", "nombre")
                    )          



            <span>	@Html.ActionLink("Home"	    , "Index"	, "Home")</span>
            <p>
                <input type="submit" value="Seleccionar Torneo" />
            </p>
</fieldset>
}